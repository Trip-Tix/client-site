interface SeatFormEntryProps {
    row: number;
    column: number;
}

export default function SeatFormEntry({row, column }: SeatFormEntryProps) {
    return (
        <div>
            Hello
        </div>
    );
}
